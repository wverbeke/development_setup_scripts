# Set vim as the default text editor for operating the terminal.
# I.e. the terminal can be controlled with vim commands.
set -o vi

# Set vim as the default text editor when opening files.
export VISUAL=vim
export EDITOR="$VISUAL"




